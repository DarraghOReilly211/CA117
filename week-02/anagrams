#!/usr/bin/env python3

import sys

def list(word):
   return [char for char in word]

for line in sys.stdin:
   split = line.split()
   a = list(split[0])
   b = list(split[1])
   a.sort()
   b.sort()
   print(a == b)
